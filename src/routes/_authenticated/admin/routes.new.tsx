import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/_authenticated/admin/routes/new')({
  component: () => <div>Editor de Roteiro em construção...</div>,
});
